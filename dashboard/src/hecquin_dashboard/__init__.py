"""Hecquin dashboard — FastAPI + Jinja2 + Chart.js.

Visualises learning data (sessions, vocab, pronunciation) and API call
telemetry (outbound LLM/embedding + inbound dashboard requests) written by
the C++ runtime into the shared SQLite `LearningStore`.
"""

__version__ = "0.1.0"
